[project]
name = "pystars"
version = "0.1.0"
description = "Automated significance testing for common biological and life sciences data."
readme = "README.md"
authors = [
    { name = "Luca Masin", email = "luca.masin@kuleuven.be" }
]
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.0",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pingouin>=0.6.1",
    "rich>=15.0.0",
    "scikit-posthocs>=0.14.0",
]

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pytest>=9.1.1",
    "ruff>=0.15.18",
    "seaborn>=0.13.2",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["examples/*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]


[tool.pytest.ini_options]
testpaths = ["tests"]
